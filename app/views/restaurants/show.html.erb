
<div class="container my-1 mx-5">
  <h1><%= @restaurant.name %></h1>
  <div class="bg-light p-3 border mt-3">
    <p class="fs-2 "><%= @restaurant.address %></p>
  </div>
  <div class="row mt-3">
    <%= link_to "Back", restaurants_path %>
    <%= link_to "Edit this task", restaurant_path(@restaurant) %>
    <%= link_to "delete this task", restaurant_path(@restaurant),  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
  </div>
</div>
